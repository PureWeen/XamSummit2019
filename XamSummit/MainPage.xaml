<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:d="http://xamarin.com/schemas/2014/forms/design" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d" 
             x:Class="XamSummit.MainPage"
             Padding="0,44,0,0"
             Visual="Material"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <StackLayout Grid.Row="0">
        <!-- Place new controls here -->
            <Entry x:Name="UserName"></Entry>
            <Entry x:Name="Password"></Entry>
            <Button x:Name="Login" Text="Login"  />
            <Button x:Name="Logout" Text="Logout"/>
            <Button x:Name="Cancel" Text="Cancel" ></Button>
        </StackLayout>
        
        <ListView Grid.Row="1" x:Name="ObservableMessages">
        </ListView>
    </Grid>
</ContentPage>